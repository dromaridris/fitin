name: smart_recipe_nutrition_app
description: "FITIN by LARC — recipe & nutrition companion, London Aesthetics & Rejuvenation Centre"
publish_to: "none"
version: 0.5.0+5

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/larc_logo.png
